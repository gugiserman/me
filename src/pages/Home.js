import React from 'react'
import PropTypes from 'prop-types'

import {
  Header,
  Intro,
  CurrentWork,
} from 'components/'

const HomePage = () => (
  <div>
    <Header />
    <Intro />
    <CurrentWork />
  </div>
)

export default HomePage
