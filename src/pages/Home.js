import React from 'react'
import PropTypes from 'prop-types'

import {
  Header,
  Intro,
} from 'components/'

const HomePage = () => (
  <div>
    <Header />
    <Intro />
  </div>
)

export default HomePage
